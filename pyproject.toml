[project]
name = "telegram-anon-bot"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = [
    "aiogram==3.15.0",
    "asyncpg==0.30.0",
    "python-dotenv>=1.0.0",
    "fastapi==0.115.6",
    "uvicorn==0.32.1",
]

[tool.vercel]
entrypoint = "api.webhook:app"
